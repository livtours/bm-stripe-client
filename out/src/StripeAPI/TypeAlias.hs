-- CHANGE WITH CAUTION: This is a generated code file generated by https://github.com/Haskell-OpenAPI-Code-Generator/Haskell-OpenAPI-Client-Code-Generator.

{-# LANGUAGE OverloadedStrings #-}
{-# LANGUAGE MultiWayIf #-}

-- | Contains all types with cyclic dependencies (between each other or to itself)
module StripeAPI.TypeAlias where

import qualified Prelude as GHC.Integer.Type
import qualified Prelude as GHC.Maybe
import qualified Control.Monad.Fail
import qualified Data.Aeson
import qualified Data.Aeson as Data.Aeson.Encoding.Internal
import qualified Data.Aeson as Data.Aeson.Types
import qualified Data.Aeson as Data.Aeson.Types.FromJSON
import qualified Data.Aeson as Data.Aeson.Types.ToJSON
import qualified Data.Aeson as Data.Aeson.Types.Internal
import qualified Data.ByteString.Char8
import qualified Data.ByteString.Char8 as Data.ByteString.Internal
import qualified Data.Foldable
import qualified Data.Functor
import qualified Data.Maybe
import qualified Data.Scientific
import qualified Data.Text
import qualified Data.Text.Internal
import qualified Data.Time.Calendar as Data.Time.Calendar.Days
import qualified Data.Time.LocalTime as Data.Time.LocalTime.Internal.ZonedTime
import qualified GHC.Base
import qualified GHC.Classes
import qualified GHC.Int
import qualified GHC.Show
import qualified GHC.Types
import qualified StripeAPI.Common


-- | Defines an alias for the schema located at @components.schemas.transform_quantity@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @transform_quantity@ is not whitelisted.
type TransformQuantity = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.transfer_data@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @transfer_data@ is not whitelisted.
type TransferData = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.transfer@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @transfer@ is not whitelisted.
type Transfer = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.test_helpers.test_clock@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @test_helpers.test_clock@ is not whitelisted.
type TestHelpers'testClock = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.tax_rate@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @tax_rate@ is not whitelisted.
type TaxRate = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.tax_id@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @tax_id@ is not whitelisted.
type TaxId = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.subscription@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @subscription@ is not whitelisted.
type Subscription = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_wechat@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_wechat@ is not whitelisted.
type SourceTypeWechat = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_three_d_secure@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_three_d_secure@ is not whitelisted.
type SourceTypeThreeDSecure = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_sofort@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_sofort@ is not whitelisted.
type SourceTypeSofort = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_sepa_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_sepa_debit@ is not whitelisted.
type SourceTypeSepaDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_p24@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_p24@ is not whitelisted.
type SourceTypeP24 = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_multibanco@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_multibanco@ is not whitelisted.
type SourceTypeMultibanco = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_klarna@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_klarna@ is not whitelisted.
type SourceTypeKlarna = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_ideal@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_ideal@ is not whitelisted.
type SourceTypeIdeal = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_giropay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_giropay@ is not whitelisted.
type SourceTypeGiropay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_eps@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_eps@ is not whitelisted.
type SourceTypeEps = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_card_present@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_card_present@ is not whitelisted.
type SourceTypeCardPresent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_card@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_card@ is not whitelisted.
type SourceTypeCard = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_bancontact@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_bancontact@ is not whitelisted.
type SourceTypeBancontact = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_au_becs_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_au_becs_debit@ is not whitelisted.
type SourceTypeAuBecsDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_alipay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_alipay@ is not whitelisted.
type SourceTypeAlipay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_acss_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_acss_debit@ is not whitelisted.
type SourceTypeAcssDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_ach_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_ach_debit@ is not whitelisted.
type SourceTypeAchDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_type_ach_credit_transfer@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_type_ach_credit_transfer@ is not whitelisted.
type SourceTypeAchCreditTransfer = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_redirect_flow@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_redirect_flow@ is not whitelisted.
type SourceRedirectFlow = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_receiver_flow@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_receiver_flow@ is not whitelisted.
type SourceReceiverFlow = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_owner@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_owner@ is not whitelisted.
type SourceOwner = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_order@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_order@ is not whitelisted.
type SourceOrder = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source_code_verification_flow@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source_code_verification_flow@ is not whitelisted.
type SourceCodeVerificationFlow = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.source@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @source@ is not whitelisted.
type Source = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.shipping@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @shipping@ is not whitelisted.
type Shipping = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.setup_intent@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @setup_intent@ is not whitelisted.
type SetupIntent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.rule@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @rule@ is not whitelisted.
type Rule = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.review@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @review@ is not whitelisted.
type Review = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.refund@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @refund@ is not whitelisted.
type Refund = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.recurring@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @recurring@ is not whitelisted.
type Recurring = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.recipient@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @recipient@ is not whitelisted.
type Recipient = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.radar_radar_options@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @radar_radar_options@ is not whitelisted.
type RadarRadarOptions = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.quote@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @quote@ is not whitelisted.
type Quote = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.promotion_code@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @promotion_code@ is not whitelisted.
type PromotionCode = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.product@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @product@ is not whitelisted.
type Product = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.price_tier@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @price_tier@ is not whitelisted.
type PriceTier = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.price@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @price@ is not whitelisted.
type Price = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_wechat_pay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_wechat_pay@ is not whitelisted.
type PaymentMethodOptionsWechatPay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_sofort@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_sofort@ is not whitelisted.
type PaymentMethodOptionsSofort = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_promptpay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_promptpay@ is not whitelisted.
type PaymentMethodOptionsPromptpay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_paynow@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_paynow@ is not whitelisted.
type PaymentMethodOptionsPaynow = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_p24@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_p24@ is not whitelisted.
type PaymentMethodOptionsP24 = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_oxxo@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_oxxo@ is not whitelisted.
type PaymentMethodOptionsOxxo = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_konbini@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_konbini@ is not whitelisted.
type PaymentMethodOptionsKonbini = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_klarna@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_klarna@ is not whitelisted.
type PaymentMethodOptionsKlarna = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_interac_present@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_interac_present@ is not whitelisted.
type PaymentMethodOptionsInteracPresent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_ideal@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_ideal@ is not whitelisted.
type PaymentMethodOptionsIdeal = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_grabpay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_grabpay@ is not whitelisted.
type PaymentMethodOptionsGrabpay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_giropay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_giropay@ is not whitelisted.
type PaymentMethodOptionsGiropay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_fpx@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_fpx@ is not whitelisted.
type PaymentMethodOptionsFpx = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_customer_balance_bank_transfer@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_customer_balance_bank_transfer@ is not whitelisted.
type PaymentMethodOptionsCustomerBalanceBankTransfer = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_customer_balance@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_customer_balance@ is not whitelisted.
type PaymentMethodOptionsCustomerBalance = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_card_present@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_card_present@ is not whitelisted.
type PaymentMethodOptionsCardPresent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_card_mandate_options@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_card_mandate_options@ is not whitelisted.
type PaymentMethodOptionsCardMandateOptions = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_card_installments@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_card_installments@ is not whitelisted.
type PaymentMethodOptionsCardInstallments = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_boleto@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_boleto@ is not whitelisted.
type PaymentMethodOptionsBoleto = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_bancontact@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_bancontact@ is not whitelisted.
type PaymentMethodOptionsBancontact = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_bacs_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_bacs_debit@ is not whitelisted.
type PaymentMethodOptionsBacsDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_alipay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_alipay@ is not whitelisted.
type PaymentMethodOptionsAlipay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_afterpay_clearpay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_afterpay_clearpay@ is not whitelisted.
type PaymentMethodOptionsAfterpayClearpay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_options_affirm@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_options_affirm@ is not whitelisted.
type PaymentMethodOptionsAffirm = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_wechat_pay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_wechat_pay@ is not whitelisted.
type PaymentMethodDetailsWechatPay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_wechat@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_wechat@ is not whitelisted.
type PaymentMethodDetailsWechat = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_us_bank_account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_us_bank_account@ is not whitelisted.
type PaymentMethodDetailsUsBankAccount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_stripe_account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_stripe_account@ is not whitelisted.
type PaymentMethodDetailsStripeAccount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_sofort@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_sofort@ is not whitelisted.
type PaymentMethodDetailsSofort = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_sepa_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_sepa_debit@ is not whitelisted.
type PaymentMethodDetailsSepaDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_promptpay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_promptpay@ is not whitelisted.
type PaymentMethodDetailsPromptpay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_paynow@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_paynow@ is not whitelisted.
type PaymentMethodDetailsPaynow = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_p24@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_p24@ is not whitelisted.
type PaymentMethodDetailsP24 = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_oxxo@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_oxxo@ is not whitelisted.
type PaymentMethodDetailsOxxo = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_multibanco@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_multibanco@ is not whitelisted.
type PaymentMethodDetailsMultibanco = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_link@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_link@ is not whitelisted.
type PaymentMethodDetailsLink = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_konbini@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_konbini@ is not whitelisted.
type PaymentMethodDetailsKonbini = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_klarna@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_klarna@ is not whitelisted.
type PaymentMethodDetailsKlarna = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_interac_present@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_interac_present@ is not whitelisted.
type PaymentMethodDetailsInteracPresent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_ideal@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_ideal@ is not whitelisted.
type PaymentMethodDetailsIdeal = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_grabpay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_grabpay@ is not whitelisted.
type PaymentMethodDetailsGrabpay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_giropay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_giropay@ is not whitelisted.
type PaymentMethodDetailsGiropay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_fpx@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_fpx@ is not whitelisted.
type PaymentMethodDetailsFpx = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_eps@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_eps@ is not whitelisted.
type PaymentMethodDetailsEps = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_customer_balance@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_customer_balance@ is not whitelisted.
type PaymentMethodDetailsCustomerBalance = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_card_present@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_card_present@ is not whitelisted.
type PaymentMethodDetailsCardPresent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_card_installments_plan@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_card_installments_plan@ is not whitelisted.
type PaymentMethodDetailsCardInstallmentsPlan = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_card@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_card@ is not whitelisted.
type PaymentMethodDetailsCard = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_boleto@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_boleto@ is not whitelisted.
type PaymentMethodDetailsBoleto = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_bancontact@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_bancontact@ is not whitelisted.
type PaymentMethodDetailsBancontact = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_bacs_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_bacs_debit@ is not whitelisted.
type PaymentMethodDetailsBacsDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_au_becs_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_au_becs_debit@ is not whitelisted.
type PaymentMethodDetailsAuBecsDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_afterpay_clearpay@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_afterpay_clearpay@ is not whitelisted.
type PaymentMethodDetailsAfterpayClearpay = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_affirm@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_affirm@ is not whitelisted.
type PaymentMethodDetailsAffirm = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_acss_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_acss_debit@ is not whitelisted.
type PaymentMethodDetailsAcssDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_ach_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_ach_debit@ is not whitelisted.
type PaymentMethodDetailsAchDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details_ach_credit_transfer@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details_ach_credit_transfer@ is not whitelisted.
type PaymentMethodDetailsAchCreditTransfer = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method_details@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method_details@ is not whitelisted.
type PaymentMethodDetails = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_method@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_method@ is not whitelisted.
type PaymentMethod = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_type_specific_payment_method_options_client@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_type_specific_payment_method_options_client@ is not whitelisted.
type PaymentIntentTypeSpecificPaymentMethodOptionsClient = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_processing@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_processing@ is not whitelisted.
type PaymentIntentProcessing = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_us_bank_account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_us_bank_account@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsUsBankAccount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_sepa_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_sepa_debit@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsSepaDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_mandate_options_sepa_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_mandate_options_sepa_debit@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsMandateOptionsSepaDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_mandate_options_acss_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_mandate_options_acss_debit@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsMandateOptionsAcssDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_link@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_link@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsLink = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_eps@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_eps@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsEps = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_card@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_card@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsCard = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_au_becs_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_au_becs_debit@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsAuBecsDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options_acss_debit@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options_acss_debit@ is not whitelisted.
type PaymentIntentPaymentMethodOptionsAcssDebit = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_payment_method_options@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_payment_method_options@ is not whitelisted.
type PaymentIntentPaymentMethodOptions = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_wechat_pay_redirect_to_ios_app@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_wechat_pay_redirect_to_ios_app@ is not whitelisted.
type PaymentIntentNextActionWechatPayRedirectToIosApp = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_wechat_pay_redirect_to_android_app@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_wechat_pay_redirect_to_android_app@ is not whitelisted.
type PaymentIntentNextActionWechatPayRedirectToAndroidApp = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_wechat_pay_display_qr_code@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_wechat_pay_display_qr_code@ is not whitelisted.
type PaymentIntentNextActionWechatPayDisplayQrCode = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_verify_with_microdeposits@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_verify_with_microdeposits@ is not whitelisted.
type PaymentIntentNextActionVerifyWithMicrodeposits = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_redirect_to_url@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_redirect_to_url@ is not whitelisted.
type PaymentIntentNextActionRedirectToUrl = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_promptpay_display_qr_code@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_promptpay_display_qr_code@ is not whitelisted.
type PaymentIntentNextActionPromptpayDisplayQrCode = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_paynow_display_qr_code@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_paynow_display_qr_code@ is not whitelisted.
type PaymentIntentNextActionPaynowDisplayQrCode = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_konbini@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_konbini@ is not whitelisted.
type PaymentIntentNextActionKonbini = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_display_oxxo_details@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_display_oxxo_details@ is not whitelisted.
type PaymentIntentNextActionDisplayOxxoDetails = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_display_bank_transfer_instructions@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_display_bank_transfer_instructions@ is not whitelisted.
type PaymentIntentNextActionDisplayBankTransferInstructions = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_card_await_notification@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_card_await_notification@ is not whitelisted.
type PaymentIntentNextActionCardAwaitNotification = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_boleto@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_boleto@ is not whitelisted.
type PaymentIntentNextActionBoleto = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action_alipay_handle_redirect@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action_alipay_handle_redirect@ is not whitelisted.
type PaymentIntentNextActionAlipayHandleRedirect = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_next_action@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_next_action@ is not whitelisted.
type PaymentIntentNextAction = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_intent_card_processing@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_intent_card_processing@ is not whitelisted.
type PaymentIntentCardProcessing = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_flows_private_payment_methods_alipay_details@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_flows_private_payment_methods_alipay_details@ is not whitelisted.
type PaymentFlowsPrivatePaymentMethodsAlipayDetails = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_flows_automatic_payment_methods_payment_intent@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_flows_automatic_payment_methods_payment_intent@ is not whitelisted.
type PaymentFlowsAutomaticPaymentMethodsPaymentIntent = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.payment_flows_amount_details@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @payment_flows_amount_details@ is not whitelisted.
type PaymentFlowsAmountDetails = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.linked_account_options_us_bank_account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @linked_account_options_us_bank_account@ is not whitelisted.
type LinkedAccountOptionsUsBankAccount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.line_item@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @line_item@ is not whitelisted.
type LineItem = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoices_status_transitions@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoices_status_transitions@ is not whitelisted.
type InvoicesStatusTransitions = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoices_resource_invoice_tax_id@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoices_resource_invoice_tax_id@ is not whitelisted.
type InvoicesResourceInvoiceTaxId = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoices_payment_settings@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoices_payment_settings@ is not whitelisted.
type InvoicesPaymentSettings = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_transfer_data@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_transfer_data@ is not whitelisted.
type InvoiceTransferData = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_threshold_reason@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_threshold_reason@ is not whitelisted.
type InvoiceThresholdReason = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_tax_amount@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_tax_amount@ is not whitelisted.
type InvoiceTaxAmount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_setting_rendering_options@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_setting_rendering_options@ is not whitelisted.
type InvoiceSettingRenderingOptions = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_setting_customer_setting@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_setting_customer_setting@ is not whitelisted.
type InvoiceSettingCustomerSetting = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_setting_custom_field@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_setting_custom_field@ is not whitelisted.
type InvoiceSettingCustomField = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.invoice_line_item_period@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @invoice_line_item_period@ is not whitelisted.
type InvoiceLineItemPeriod = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.discounts_resource_discount_amount@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @discounts_resource_discount_amount@ is not whitelisted.
type DiscountsResourceDiscountAmount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.discount@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @discount@ is not whitelisted.
type Discount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.deleted_tax_id@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @deleted_tax_id@ is not whitelisted.
type DeletedTaxId = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.deleted_product@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @deleted_product@ is not whitelisted.
type DeletedProduct = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.deleted_discount@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @deleted_discount@ is not whitelisted.
type DeletedDiscount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.deleted_customer@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @deleted_customer@ is not whitelisted.
type DeletedCustomer = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.deleted_application@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @deleted_application@ is not whitelisted.
type DeletedApplication = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.customer_tax@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @customer_tax@ is not whitelisted.
type CustomerTax = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.customer_balance_customer_balance_settings@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @customer_balance_customer_balance_settings@ is not whitelisted.
type CustomerBalanceCustomerBalanceSettings = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.custom_unit_amount@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @custom_unit_amount@ is not whitelisted.
type CustomUnitAmount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.coupon@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @coupon@ is not whitelisted.
type Coupon = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.charge_transfer_data@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @charge_transfer_data@ is not whitelisted.
type ChargeTransferData = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.charge_outcome@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @charge_outcome@ is not whitelisted.
type ChargeOutcome = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.charge_fraud_details@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @charge_fraud_details@ is not whitelisted.
type ChargeFraudDetails = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.cash_balance@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @cash_balance@ is not whitelisted.
type CashBalance = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.card@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @card@ is not whitelisted.
type Card = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.bitcoin_transaction@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @bitcoin_transaction@ is not whitelisted.
type BitcoinTransaction = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.bitcoin_receiver@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @bitcoin_receiver@ is not whitelisted.
type BitcoinReceiver = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.billing_details@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @billing_details@ is not whitelisted.
type BillingDetails = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.bank_account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @bank_account@ is not whitelisted.
type BankAccount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.balance_transaction@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @balance_transaction@ is not whitelisted.
type BalanceTransaction = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.automatic_tax@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @automatic_tax@ is not whitelisted.
type AutomaticTax = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.application_fee@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @application_fee@ is not whitelisted.
type ApplicationFee = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.application@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @application@ is not whitelisted.
type Application = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.api_errors@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @api_errors@ is not whitelisted.
type ApiErrors = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.alipay_account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @alipay_account@ is not whitelisted.
type AlipayAccount = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.address@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @address@ is not whitelisted.
type Address = Data.Aeson.Types.Internal.Value

-- | Defines an alias for the schema located at @components.schemas.account@ in the specification.
-- 
-- This is just a type synonym and possibly could have a more precise type because the schema name @account@ is not whitelisted.
type Account = Data.Aeson.Types.Internal.Value
